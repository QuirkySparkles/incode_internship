import React from "react";
import Typography from "@material-ui/core/Typography";

const PageNotFound = () => (
  <Typography variant="display3" style={{ marginTop: "15%" }}>
    Page not found :(
  </Typography>
  );

export default PageNotFound;
